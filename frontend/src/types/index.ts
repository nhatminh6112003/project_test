export interface Beer {
  id: number;
  name: string;
  tagline: string;
  image_url: string;
}
export interface Book {
  id: number;
  point: number;
  tag: string[];
  coverImage: string;
  writer: string;
  title: string;
  quantity:number;
}
